import React from 'react';
import AppBar from 'material-ui/AppBar';
import { Toolbar } from 'material-ui/Toolbar';

export default () => (
  <div>
    <AppBar title="To do" iconClassNameRight="muidocs-icon-navigation-expand-more" />
    <Toolbar />
  </div>
);
