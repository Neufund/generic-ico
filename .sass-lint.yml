options:
  formatter: stylish
  merge-default-rules: true
  max-warnings: 0
files:
  include: '**/*.s+(a|c)ss'
  ignore:
    - 'public/**'
    - 'node_modules/**'
rules:
  property-sort-order:
    - 1
    - order: concentric
